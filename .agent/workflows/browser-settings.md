---
description: Browser settings and preferences for automated testing
---

# Browser Settings

## Script Execution
// turbo-all
When using the browser subagent, scripts should be allowed to run automatically without requiring user approval. This enables smooth automated testing and interaction with web pages.

## Usage
When performing browser automation tasks:
1. Navigate to pages normally
2. Scripts will execute automatically
3. Interact with page elements as needed
